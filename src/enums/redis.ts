enum StoreSymbol {
    blocks = 'mmc_blocks',
    txs = 'mmc_txs',
    accounts = 'mmc_accounts',
    wallets = 'mmc_wallets',
    contracts = 'mmc_contracts',
    privacy = 'mmc_privacy',
    parent_node = 'mmc_parent_node',
    children_node = 'mmc_children_node',
    node = 'mmc_node',
}

export { StoreSymbol }
